{% set name = "rollbar" %}
{% set version = "0.16.3" %}
{% set sha256 = "02313dfc60710ec736ab033d0f8c969d857a8b991cd67e0c1a91620e8a04ede2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - rollbar=rollbar.cli:main

requirements:
  host:
    - python >=3.4
    - pip
  run:
    - python >=3.4
    - requests >=0.12.1
    - six >=1.9.0

test:
  requires:
    - pip
  imports:
    - rollbar
  commands:
    - pip check

about:
  home: https://github.com/rollbar/pyrollbar
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Rollbar notifier for Python

  description: |
    Python notifier for reporting exceptions, errors, and log messages to Rollbar.
  doc_url: https://rollbar.com/docs/notifier/pyrollbar/

extra:
  recipe-maintainers:
    - fpaniagua
    - TheOriginalAlex
